<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="AStarDev.ScraperPlaying.Home.MainWindow"
        Title="AStarDev Scraper Playing"
        Width="1800"
        Height="800"
        MinWidth="480"
        MinHeight="320">
  <Grid RowDefinitions="Auto,Auto,*" Margin="32">
    <TextBlock Text="AStarDev Scraper Playing"
               FontSize="28"
               FontWeight="SemiBold" />
    <StackPanel Grid.Row="1"
                Orientation="Horizontal"
                HorizontalAlignment="Center"
                Spacing="12"
                Margin="0,24,0,0">
      <Button x:Name="ImportConfigurationButton" Content="Import configuration" Click="ImportConfiguration" />
      <Button x:Name="ExportConfigurationButton" Content="Export configuration" Click="ExportConfiguration" />
      <Button x:Name="RunScraperButton" Content="Run Scraper" Click="RunScraper" />
      <Button x:Name="CancelButton" Content="Cancel" Click="CancelOperation" IsEnabled="False" />
    </StackPanel>
    <Border Grid.Row="2"
            Margin="0,24,0,0"
            Padding="24"
            Background="{DynamicResource SystemControlBackgroundAltHighBrush}"
            CornerRadius="8">
      <ScrollViewer x:Name="StatusScrollViewer" VerticalScrollBarVisibility="Auto">
        <TextBlock Text="MainWindow is ready." x:Name="StatusTextBlock" TextWrapping="Wrap" FontSize="12" />
      </ScrollViewer>
    </Border>
  </Grid>
</Window>