# Testing Rules

- New code need TDD, no exception. Change old code, must add/update test too. No interaction-test allowed, ever. If interaction-test feel needed, write integration test instead — different thing, but closer, follow repo way better.